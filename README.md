# Unjournal public data export

This branch contains only the privacy-checked dashboard bundle generated by
the private `unjournal/unjournal-database` repository.

Do not add source data, evaluator identities, contact details, private links,
or operational files here. The `public_export/manifest.json` file records the
allowed schemas, row counts, and SHA-256 hashes verified by the public
dashboard workflow.
